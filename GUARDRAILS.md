# 📏 Guardrails & Rules — read before teaching

These are the rules for my guided course. Any Claude session should follow them until the
course is complete. (There's also a `/teach` skill that automates this — just type `/teach`.)

## The golden rules
1. **Local & private only.** Everything lives on this Mac under `Desktop/MyDevJourney/`.
   NEVER upload my work, notes, code, or portfolio to any cloud/artifact/hosted service.
2. **Never delete or overwrite my files without asking me first.** Create and move freely;
   confirm before any removal. (A folder was once deleted by mistake — don't repeat it.)
3. **Read `PROGRESS.md` first, every session**, and teach from the CURRENT STATUS.
4. **Teach like I'm 10.** Simple language, analogies, define every jargon word.
5. **Be honest.** Don't mark things done that I didn't do. Don't sugarcoat difficulty.

## Every topic follows this format
🧒 ELI5 → 💻 live code (I write the key 5–10 lines) → 🏋️ exercise in chat → 🎤 interview Q&A
→ 📝 notes file → 🎞️ slides file.

## Two daily tracks
- **Track A — Skills** (~1h): the roadmap (Android → Backend → AI/ML).
- **Track B — DSA** (~1h): daily practice, building to ~300–400 problems for MNC interviews.
- One lesson at a time; wait for me before moving on.

## After each topic (bookkeeping)
- Save notes → `notes/<NN>-<slug>.md`
- Save slides → `slides/<NN>-<slug>.html`  (for workshops)
- Update `PROGRESS.md` (tick item, update status + next action, add dated log line, DSA count)

## Projects → GitHub
- GitHub account: `Aiyappa-DigitalGreen` (gh CLI authed).
- When I actually build a portfolio project: create a repo, push the code, then update
  `portfolio/index.html` to link the real repo and flip the project status to "done".
- No fake/dead links. A project is only "Live" after it's built and pushed.

## Goal (context)
Target roles: **AI Engineer + Android** (primary), broad in Backend. Company target:
**product/MNC** → DSA + system design are top priority. Timeline: ~10–11 months at ~2h/day.
